### 配置 user
fawefaw

```
git config --global user.name swint
git config --global user.email swinting@163.com

--local：只对某一个仓库有效
--global：对当前用户的所有仓库有效
--system：对系统所有登录的用户有效

git config --list --local|--global|--system：显示配置
`lll``

aaa
bbb
ccc
